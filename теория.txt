
npm create vite@latest - установка сборщика vite (в консоли)

npm i = установка зависимостей

(1) Логическое && всегда возвращает первое ложное выражение и если такового нет, 
то последнее выражение вне зависимости ложное оно или правдивое.
(2) Логическое || всегда возвращает первое истинное выражение, либо последнее 
вне зависимости ложное оно или нет

деструктуризация объектов = const {name: userName, age: userAge} = user
деструктуризация массивов = const [first, second, third] = [10, 20, 30]

ПРЕДИКАТ - ФУНКЦИЯ, КОТОРАЯ ВОЗВРАЩАЕТ true ИЛИ false.

CALLBACK ФУНКЦИЯ -  это функция, переданная в другую функцию в качестве аргумента, 
которая затем вызывается по завершении какого-либо действия. 

O(n) — обозначение линейной сложности временной сложности алгоритма. 
Это означает, что время выполнения алгоритма растёт прямо пропорционально 
размеру входных данных (n). Нормальная сложность алгоритма

Fiber Node (файбер нод) - узел, в котором хранятся данные

хайк о хежен - что между собой очень связано

Рефакторинг — это процесс изменения внутренней структуры программного кода без изменения
его внешнего поведения. Цель — сделать код более понятным, читаемым и удобным для дальнейшей поддержки и развития.

лов коплинг - низкая связанность

рендер в реакте - вызов компонента чтобы получить актуальный jsx/tsx

«Информационным экспертом» - выступает компонент, который владеет необходимыми данными (state, props, context).

Событийный проп - когда родитель передает в пропс callback функцию своему ребенку и дальше взависимости от логики
ребенок вызывает данную callback функцию (можно передать в нее параметры)

Инверсия управления (Inversion of Control, IoC) — это архитектурный паттерн, при котором объект, от которого мы зависим, 
начинает управлять нами, а не как обычно — мы им. Происходит, когда библиотека или фреймворк управляет нашим кодом.

Prop Drilling - Ситуация, когда данные (пропсы) передаются через несколько промежуточных компонентов, 
которые не используют эти данные, а только транслируют их дальше по иерархии.

Декомпозиция - когда большой компонент дробиться или преобразуется в несколько маленьких компонентов,
и они тоже могут превращаться в еще более мелкие компоненты и т.д.

Compilition (компиляция) - процесс превращения ts файлов в js файлы

npx tsc -w - команда для автоматической компиляции в терминале
node --watch (путь к файлу) - показ компиляции или выполнение кода js в консоли

npm run build - команда для отслеживания ошибок 

Принцип единственной ответственности (Single Responsibility Principle, SRP) - Компонент или модуль должен отвечать только за одну 
задачу. Если необходимо изменить внешний вид, не должны затрагиваться логика данных или структура запросов. Мы, как программисты, 
хотим расширять систему, но не модифицировать уже написанный код.

Разделение ответственностей (Separation of Concerns) - это архитектурный принцип, согласно которому система должна быть разделена 
на независимые аспекты (зоны ответственности) (concerns): отрисовка интерфейса, управление состоянием, сетевые запросы, 
бизнес-логика и т.д. Каждый concern должен решать одну задачу и знать минимум о других.

concern - отдельная ответственность, аспект: отрисовка интерфейса, управление состоянием, сетевые запросы, 
бизнес-логика и т.д. Каждый concern должен решать одну задачу и знать минимум о других.

Слой (layer) — это практическое воплощение определённого concern в структуре проекта, например, в виде папки.

🖥️ Presentation Layer — презентационный слой — отвечает за отображение данных и взаимодействие с пользователем (UI). 
Идеально, если этот слой «тонкий»: содержит только код интерфейса и не выполняет бизнес-логику.

⚙️ Business Logic Layer (BLL) — слой бизнес-логики — управляет состоянием и данными, реализует правила и алгоритмы.

🗃️ Data Access Layer (DAL) — отвечает за работу с API и хранилищами. Содержит логику запросов и не зависит от фреймворков, 
что позволяет переиспользовать его, например, и в React, и в Angular проектах.

Рефакторинг — это улучшение структуры кода на основе архитектурных принципов без изменения 
видимого поведения программы для пользователя.

Структура данных — это способ хранения и организации данных, чтобы эффективно ими пользоваться в алгоритмах.

1. Массив — место, где данные хранятся в определённом порядке и к ним можно обратиться по индексу.
2. Очередь (FIFO — первый пришёл, первый ушёл) — данные обрабатываются строго в порядке поступления, как очередь в магазине.
3. Стек (LIFO — последний пришёл, первый ушёл) — данные кладутся и забираются с одного конца, 
пример — стопка тарелок, верхняя тарелка будет взята первой.

Call stack (стек вызовов) — это структура данных, которая используется для отслеживания выполнения функций в программе. 
Она позволяет интерпретатору (например, движку JavaScript в браузере) знать, какая функция выполняется в данный момент, 
какие функции вызываются изнутри неё, а также определять, какая функция будет вызвана следующей

Кастомный хук — это пользовательская функция в React, которая позволяет инкапсулировать и повторно использовать 
логику (ИМЕННО ЛОГИКУ) в разных компонентах. Он должен содержать встроенные хуки React (например, useState, useEffect, useMemo)
и предназначен для использования внутри функциональных компонентов

🧩 Отрисовка UI (визуализация) — генерирует JSX-разметку, видимую пользователю.
🔄 Управление состоянием (state management) — хранит и обновляет данные (например, список треков).
🌐 Выполнение запросов к API (API requests) — взаимодействует с сервером.


🧱2 ТРИ СЛОЯ АРХИТЕКТУРЫ React-ПРИЛОЖЕНИЯ:

🎨 2.1. Слой UI (Presentation Layer)
📋 Задача: отображение данных и реакция на действия пользователя.
🧱 Состав: React-компоненты с JSX.
✨ Результат: “тонкие” компоненты — не содержат логику, только визуализацию.

⚙️ 2.2. Слой бизнес-логики (Business Logic Layer) bbl/state management
🧠 Задача: управление состоянием и реализация бизнес-правил.
🪄 Реализация: кастомные хуки (useTracks, useCounter и т.д.).
🧩 Альтернативные названия: domain, state layer, application logic layer.

🌐 2.3. Слой доступа к данным (Data Access Layer) dal/api
🔗 Задача: взаимодействие с сервером (API).
🧱 Состав: отдельные функции или объекты для запросов (getTracksViaAPI).
🕶️ Преимущество: UI не знает о структуре API, токенах и URL — всё инкапсулировано.\

Хэш-функция — это однонаправленный, воспроизводимый алгоритм, который преобразует строку 
произвольной длины в строку фиксированной длины. Поскольку путь к каждому файлу в проекте уникален, 
сгенерированный хэш также будет уникальным, что и предотвращает коллизии имен.

CLSX - Генерация уникальных имен классов — это только половина дела. Их условное и эффективное 
применение в логике компонента является критически важным последним шагом. Ручная конкатенация 
строк для формирования атрибута className является хрупкой и подверженной ошибкам, что требует 
использования утилитного подхода.

npm install --save clsx = установка clsx

npx serve . = вбивать в консоли. Создает сервер на основе текущей дериктории

npx serve . -s = вбивать в консоли. Создает сервер на основе текущей дериктории, НО ТОЛЬКО В РЕЖИМЕ SPA

SPA - single page application: это тип веб-приложения, которое загружается один раз и динамически обновляет 
содержимое страницы при взаимодействии с пользователем без полной перезагрузки. Это позволяет создать плавный и 
отзывчивый пользовательский опыт, близкий к нативным приложениям. 

CI — Continuous Integration (Непрерывная интеграция)
Разработчики часто отправляют (пушат) код в общий репозиторий — и каждый пуш автоматически запускает проверки.

CD — Continuous Delivery / Deployment (Непрерывная доставка / развертывание)
Код проходит сборку, тестирование и автоматически доставляется на сервер после успешной интеграции.

Чтобы понять архитектуру, представим её как систему из трёх компонентов:

1. Git-сервер (GitHub) Хранилище кода: TSX, CSS-модули, конфиги и т.д.
2. CI/CD-сервер (GitHub Actions) Оркестратор, который реагирует на пуши, забирает код и выполняет пайплайн.
3. Хостинг-сервер (GitHub Pages) Конечная точка, на которую выкладывается папка dist.

